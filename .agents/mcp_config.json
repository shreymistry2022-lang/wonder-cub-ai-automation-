{
  "mcpServers": {
    "sqlite-database": {
      "command": "node",
      "args": ["-e", "console.log('SQLite MCP Ready')"],
      "env": {
        "DATABASE_URL": "data/thewondercub.db"
      }
    }
  }
}
