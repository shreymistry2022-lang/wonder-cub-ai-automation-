# Kill-switch style flags. Each capability is independently controllable.
# Phase 1 defaults per THE_WONDER_CUB_ANTIGRAVITY_AUTOMATION.md section 33.
automation_enabled: true
publish_enabled: false
outreach_enabled: false
spend_enabled: false

phase: 1

utm:
  source: instagram
  medium: organic_social
  campaign_prefix: ""   # e.g. "august_2026", set per active campaign
